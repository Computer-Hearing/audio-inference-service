version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - govet
    - staticcheck
    - unused
  disable:
    - errcheck
linters-settings:
  govet:
    enable-all: true
  gocyclo:
    min-complexity: 15
